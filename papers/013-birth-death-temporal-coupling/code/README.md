# Code

Planned first artifact: `pilot0_nvss_1988.py`.

It will validate the source file and required variables, construct birth/death dates with explicit leap-day rules, compute circular phase differences, estimate birthday-window effects, run structure-preserving null permutations, and export machine-readable results.

Traditional-calendar features will not enter confirmatory testing until the feature dictionary is frozen.
